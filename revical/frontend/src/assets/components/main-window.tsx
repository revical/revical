import { Component, h } from "preact";

interface MainWindowProps { }

interface MainWindowState { }

export class MainWindow extends Component<MainWindowProps, MainWindowState> {
    render() {
        return (
            <div id="main-window">
                a
            </div>
        );
    }
}